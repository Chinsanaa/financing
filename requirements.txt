# Core ML pipeline dependencies (src/ + tests).
# The deployed backend pins its own versions in backend/requirements.txt.
pandas>=1.5.0
scikit-learn>=1.0.0
jieba>=0.42.1
openpyxl>=3.0.0
joblib>=1.1.0
model2vec>=0.3.0
